<UserControl x:Class="CatGame.Views.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CatGame.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Background>
        <ImageBrush ImageSource="/Views/фон главное.jpg" Stretch="UniformToFill"/>
    </UserControl.Background>
    <Grid>
        <StackPanel>
            <StackPanel Width="230" Height="168" RenderTransformOrigin="0.5,0.5">
                <StackPanel.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform/>
                        <RotateTransform/>
                        <TranslateTransform X="17" Y="220"/>
                    </TransformGroup>
                </StackPanel.RenderTransform>
                <Button Content="Играть" Command="{Binding PlayCommand}" Width="150" Height="37" Margin="5" BorderBrush="#FFF19EBC" BorderThickness="2" FontFamily="Montserrat Medium">
                    <Button.Background>
                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                            <GradientStop Color="#FFF99FC1"/>
                            <GradientStop Color="White" Offset="1"/>
                            <GradientStop Color="#FFF19EBC"/>
                        </LinearGradientBrush>
                    </Button.Background>
                </Button>
                <Button Content="Правила" Command="{Binding RulesCommand}" Width="150" Height="40" Margin="5" BorderBrush="#FFF19EBC" BorderThickness="2" FontFamily="Montserrat Medium">
                    <Button.Background>
                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                            <GradientStop Color="#FFF19EBC"/>
                            <GradientStop Color="#FFFFFEFE" Offset="1"/>
                        </LinearGradientBrush>
                    </Button.Background>
                </Button>

                <Button Content="Выход" Command="{Binding ExitCommand}" Width="150" Height="40" Margin="5" BorderBrush="#FFF19EBC" BorderThickness="2" FontFamily="Montserrat Medium">
                    <Button.Background>
                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                            <GradientStop Color="#FFF19EBC"/>
                            <GradientStop Color="#FFFEFFFD" Offset="1"/>
                        </LinearGradientBrush>
                    </Button.Background>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>